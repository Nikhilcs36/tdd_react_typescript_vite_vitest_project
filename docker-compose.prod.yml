services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: frontend-prod
    ports:
      - "3000:80"
    networks:
      - tdd-network

networks:
  tdd-network:
    external: true
    name: tdd-network